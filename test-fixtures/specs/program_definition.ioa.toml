# ProgramDefinition entity -- minimal state machine to enable actor-backed CRUD
# through /tdata/ProgramDefinitions.

[automaton]
name = "ProgramDefinition"
states = ["Ready"]
initial = "Ready"

[[action]]
name = "Touch"
kind = "input"
from = ["Ready"]
to = "Ready"
