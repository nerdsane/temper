# ARN-237: plan

Inspect decoding callers; add a regression for an invalid entity in an optional attribute; make decoding failure explicit; run CSDL tests and relevant metadata flow checks, then review the final diff. Keep deferred strict-parser requirements on ARN-237. Merge through recorded proof/review and verify the applicable kernel deployment.
