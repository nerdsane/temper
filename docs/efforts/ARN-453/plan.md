# ARN-453 — Plan
One temper PR (+ one stack commit for the canonical combined gate workflow and
the two scripts that re-run gates by name). Proof: the PR's own CI run on the
new workflow set (every kept job green; the combined sdlc workflow reporting
the four contexts), plus the README badges rendering live values.
