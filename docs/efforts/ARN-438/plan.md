# Plan - temper side of ARN-438

1. Worktree off `origin/main`, branch `claude/arn-438-verify-temper`. [done]
2. Ground every surface in kernel source (parallel research; cite file:line). [done]
3. Author 8 new + 2 rewritten feature files + SKILL.md + README, verify-temperpaw
   shape, corrections encoded. [done]
4. Delete `.github/workflows/deploy-observe.yml`. [done]
5. Commit the design chain (this folder). [in progress]
6. Live verification: boot with the skill recipe, run cascade + a dst_ suite,
   capture evidence. [next]
7. Draft PR early; ping the lead; panel at 3/3; fix findings; land.

## Expected end state

`verify-temper` maps the kernel's real surfaces (13 files), the two numeric claims
are corrected, the launch recipe isolates state, and the inert deploy workflow is
gone. One temper PR through the vendored SDLC gates.
