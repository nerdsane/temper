# ARN-478: spec

Accept the immutable typed-reference design without claiming it is implemented. State the known directed-reachability limitation of ADR-0150. Activation audits must report incomplete and block if required set-once history cannot be reconstructed, including data-only records lacking history. No runtime changes are included.
