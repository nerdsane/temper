# Deprecated: Barebones `/v1/agents` API Spec

This document is superseded.

Current direction for this service:
- Agent APIs are CSDL-based and exposed through `/tdata`.
- See `docs/agent_api_csdl.md` for the canonical spec.
