[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'temper serve --port $PORT --storage turso'"
healthcheckPath = "/tdata"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
