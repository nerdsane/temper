{
  "batch": "Cross-cutting Sweep",
  "batch_index": 7,
  "assessments": {
    "authorization_consistency": 72.0
  },
  "dimension_notes": {
    "authorization_consistency": {
      "evidence": [
        "`SecurityContext::from_headers` accepts `x-temper-principal-kind: system` from request headers (`crates/temper-authz/src/context.rs:65-74`), and `AuthzEngine::authorize_or_bypass` unconditionally allows system principals (`crates/temper-authz/src/engine.rs:217-229`).",
        "`extract_tenant` silently falls back to first registered tenant or `TenantId::default()` when `x-tenant-id` is absent (`crates/temper-server/src/odata/common.rs:13-37`), creating implicit tenant selection in a multi-tenant server.",
        "Observe authorization is action-only with empty resource attrs (`crates/temper-server/src/authz_helpers.rs:60-65`), while handlers return cross-tenant data directly (e.g., all actor keys in `crates/temper-server/src/observe/entities.rs:30-51`, all tenant specs in `crates/temper-server/src/observe/specs.rs:30-67`).",
        "Authz telemetry records tenant as literal `\"default\"` in both auth paths (`crates/temper-server/src/state/entity_ops.rs:230-237` and `277-284`), weakening denial/audit consistency for non-default tenants."
      ],
      "impact_scope": "codebase",
      "fix_scope": "architectural_change",
      "confidence": "high"
    }
  },
  "findings": [
    {
      "dimension": "authorization_consistency",
      "identifier": "spoofable_system_principal_header",
      "summary": "Request headers can self-assign `System` principal and bypass all authorization checks",
      "related_files": [
        "crates/temper-authz/src/context.rs",
        "crates/temper-authz/src/engine.rs",
        "crates/temper-server/src/authz_helpers.rs"
      ],
      "evidence": [
        "`SecurityContext::from_headers` maps `x-temper-principal-kind=system` to `PrincipalKind::System` (`context.rs:65-74`).",
        "`authorize_or_bypass` returns `Allow` for any system principal without policy evaluation (`engine.rs:217-229`).",
        "Server request path builds security context directly from inbound `x-temper-*` headers (`authz_helpers.rs:17-40`) with no in-process trust-boundary guard."
      ],
      "suggestion": "Remove acceptance of `system` from external header parsing and reserve system principals for trusted construction (`SecurityContext::system()` on internal call paths only). Add a server-side middleware that strips or rejects privileged `x-temper-*` identity headers from untrusted requests, and add tests proving external requests cannot obtain system/admin bypass by header spoofing.",
      "confidence": "high",
      "impact_scope": "codebase",
      "fix_scope": "architectural_change",
      "root_cause_cluster": "untrusted_identity_headers"
    },
    {
      "dimension": "authorization_consistency",
      "identifier": "implicit_tenant_fallback_authorization_drift",
      "summary": "Missing tenant header silently falls back to arbitrary/default tenant instead of explicit rejection",
      "related_files": [
        "crates/temper-server/src/odata/common.rs",
        "crates/temper-server/src/observe/entities.rs"
      ],
      "evidence": [
        "`extract_tenant` chooses first registered tenant when `x-tenant-id` is missing, then falls back to `TenantId::default()` (`odata/common.rs:13-37`).",
        "`get_entity_history` uses this derived tenant for actor lookup and event-store reads (`observe/entities.rs:64-106`).",
        "This makes authorization outcomes depend on registry/default ordering rather than explicit tenant identity."
      ],
      "suggestion": "Require an explicit tenant for multi-tenant endpoints: return `400`/`401` when `x-tenant-id` is missing or unknown, and only permit fallback in an explicitly configured single-tenant compatibility mode. Centralize this as a strict tenant resolver shared by OData and observe handlers.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "missing_tenant_binding_in_authz"
    },
    {
      "dimension": "authorization_consistency",
      "identifier": "observe_endpoints_not_tenant_scoped_after_auth",
      "summary": "Observe handlers authorize once but return cross-tenant data without tenant-bound filtering",
      "related_files": [
        "crates/temper-server/src/authz_helpers.rs",
        "crates/temper-server/src/observe/entities.rs",
        "crates/temper-server/src/observe/specs.rs",
        "crates/temper-server/src/events.rs"
      ],
      "evidence": [
        "`require_observe_auth` checks action/resource with empty attrs (`authz_helpers.rs:60-65`), so tenant/resource instance is not part of the decision input.",
        "`list_entities` iterates all actor registry keys and returns all entities regardless of tenant (`observe/entities.rs:30-51`).",
        "`list_specs` iterates `registry.tenant_ids()` and returns every tenant’s specs (`observe/specs.rs:30-67`).",
        "SSE events stream all tenant changes to any caller that passes one global observe permission (`events.rs:47-58`)."
      ],
      "suggestion": "Introduce tenant- and resource-scoped observe authorization: extract tenant context first, include it in Cedar context/resource attrs, and filter query/stream outputs to authorized tenants only. For cross-tenant admin views, use explicit elevated actions and separate handler paths.",
      "confidence": "high",
      "impact_scope": "codebase",
      "fix_scope": "architectural_change",
      "root_cause_cluster": "missing_tenant_binding_in_authz"
    },
    {
      "dimension": "authorization_consistency",
      "identifier": "authz_audit_tenant_hardcoded_default",
      "summary": "Authorization audit events always attribute tenant as `default`",
      "related_files": [
        "crates/temper-server/src/state/entity_ops.rs",
        "README.md"
      ],
      "evidence": [
        "Both `authorize` and `authorize_with_context` call `wide_event::from_authz_decision(..., \"default\")` (`entity_ops.rs:230-237` and `277-284`) instead of using the actual request tenant.",
        "README promises complete governance/auditability for actions (`README.md:25-39`), but hardcoded tenant attribution breaks consistency in multi-tenant traces."
      ],
      "suggestion": "Thread resolved tenant through both authorization paths and emit it in wide-event authz telemetry. Add regression tests that verify non-default tenants produce tenant-correct authz decision events.",
      "confidence": "medium",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "missing_tenant_binding_in_authz"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Identity and tenant context are treated as optional/convenience inputs instead of mandatory authorization boundary inputs.",
      "Observe auth design is coarse-grained (action-level) and not consistently bound to tenant/resource scope."
    ],
    "likely_symptoms": [
      "implicit_tenant_fallback_authorization_drift",
      "observe_endpoints_not_tenant_scoped_after_auth",
      "authz_audit_tenant_hardcoded_default"
    ],
    "possible_false_positives": [
      "If deployment always enforces a trusted header-stripping gateway before this server, spoofable system header risk may be reduced, but the code-level trust assumption remains undocumented/enforced in-process."
    ]
  }
}