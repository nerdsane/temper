{
  "batch": "Governance & Contracts",
  "batch_index": 5,
  "assessments": {
    "cross_module_architecture": 83.5,
    "high_level_elegance": 87.0,
    "package_organization": 82.5
  },
  "dimension_notes": {
    "cross_module_architecture": {
      "evidence": [
        "`records_list` routes are hard-wired to `state.persistent_store()` and explicitly documented as Turso-only (`crates/temper-server/src/observe/evolution/records_list.rs`:27,95), with empty fallback when absent (`:77-85`, `:131-135`).",
        "`connect_postgres_store` still provisions and migrates evolution persistence (`crates/temper-cli/src/serve/storage.rs`:49-53), indicating a second backend path that is not part of observe read routing.",
        "README positions Postgres and Turso as first-class persistent backends and marks Evolution Engine as implemented (`README.md`:41,216,219), but the runtime read contract in observe is backend-specific."
      ],
      "impact_scope": "subsystem",
      "fix_scope": "architectural_change",
      "confidence": "high"
    },
    "high_level_elegance": {
      "evidence": [
        "README states strict governance claims like “Every spec passes four levels... No exceptions” (`README.md`:111) and invariant-level guarantees (`README.md`:120).",
        "Restore path can synthesize `VerificationStatus::Completed` from coarse persisted flags even when no structured `verification_result` exists (`crates/temper-cli/src/serve/storage.rs`:172-207), creating placeholder level summaries (`:187-191`) rather than replaying original verification artifacts.",
        "This creates a docs-vs-runtime contract gap around what 'verified' means after persistence/restore."
      ],
      "impact_scope": "codebase",
      "fix_scope": "multi_file_refactor",
      "confidence": "high",
      "issues_preventing_higher_score": "Verification/governance guarantees are communicated as absolute in docs, but restore semantics permit reconstructed verification outcomes that may not preserve original verification evidence fidelity."
    },
    "package_organization": {
      "evidence": [
        "The structure already has a dedicated `crates/temper-server/src/observe/evolution/` package (4 files, 975 LOC in `holistic_context.structure.directory_profiles`), but insight lifecycle logic remains in root-level `crates/temper-server/src/insight_generator.rs`.",
        "`records_list` and `insight_generator` operate on the same evolution/insight domain concepts but are split across unrelated package locations (`crates/temper-server/src/observe/evolution/records_list.rs` and `crates/temper-server/src/insight_generator.rs`).",
        "`crates/temper-server/src/observe/` is already at 10 files / 3450 LOC in `directory_profiles`, with evolution concerns distributed between root and nested directories, reducing navigability and ownership clarity."
      ],
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "confidence": "medium"
    }
  },
  "findings": [
    {
      "dimension": "cross_module_architecture",
      "identifier": "evolution_backend_contract_split",
      "summary": "Evolution read APIs are Turso-bound while startup provisions Postgres evolution storage",
      "related_files": [
        "crates/temper-server/src/observe/evolution/records_list.rs",
        "crates/temper-cli/src/serve/storage.rs",
        "README.md"
      ],
      "evidence": [
        "Observe handlers query only `state.persistent_store()` with comments calling Turso the single source (`records_list.rs`:27,95) and return empty data when no persistent store is present (`:77-85`, `:131-135`).",
        "Postgres startup path migrates `evolution_records` via `PostgresRecordStore` (`storage.rs`:49-53), but that backend is not consumed by these read APIs.",
        "README advertises Postgres/Turso persistence parity and implemented evolution pipeline (`README.md`:41,216,219), so backend behavior is contractually ambiguous."
      ],
      "suggestion": "Introduce a backend-neutral evolution read interface in `temper-server` (e.g., `EvolutionReadStore` with `list_evolution_records` and `list_ranked_insights`) and provide adapters for Turso and Postgres record stores. Wire `ServerState` to this trait instead of Turso-specific accessors, then add backend-matrix tests (`cargo test -p temper-server evolution`, with both Postgres and Turso fixtures) to enforce compatibility policy.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "architectural_change",
      "root_cause_cluster": "backend_contract_split"
    },
    {
      "dimension": "high_level_elegance",
      "identifier": "verification_claims_exceed_restore_semantics",
      "summary": "Verification guarantees in docs are stronger than persisted restore semantics",
      "related_files": [
        "README.md",
        "crates/temper-cli/src/serve/storage.rs"
      ],
      "evidence": [
        "Docs present verification as strict and exhaustive before deployment (`README.md`:111-120).",
        "`row_to_registry_status` reconstructs completed verification from status/booleans when detailed results are absent (`storage.rs`:176-207), fabricating generic level summaries (`:187-191`) rather than requiring original evidence.",
        "This makes the runtime contract after restart materially weaker than the documented governance contract."
      ],
      "suggestion": "Define a single verification contract for restore paths: either require persisted `verification_result` as authoritative (fail closed when missing), or explicitly downgrade restored entries to a distinct non-verified status. Update README wording and API/status naming to reflect this lifecycle, and add regression tests for restore-with-missing-result behavior.",
      "confidence": "high",
      "impact_scope": "codebase",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "governance_contract_drift"
    },
    {
      "dimension": "package_organization",
      "identifier": "evolution_domain_scattered_between_root_and_observe",
      "summary": "Evolution/insight ownership is split across root and observe packages",
      "related_files": [
        "crates/temper-server/src/insight_generator.rs",
        "crates/temper-server/src/observe/evolution/records_list.rs",
        "README.md"
      ],
      "evidence": [
        "`insight_generator.rs` contains core evolution-domain generation logic, while evolution read APIs live in `observe/evolution/records_list.rs`, fragmenting one domain across package boundaries.",
        "`directory_profiles` already show a dedicated evolution package (`crates/temper-server/src/observe/evolution/`, 4 files) and a large observe surface (`crates/temper-server/src/observe/`, 10 files), yet key insight logic remains outside it.",
        "README frames evolution as a coherent engine (`README.md`:154,219), but code placement diffuses ownership."
      ],
      "suggestion": "Staged reorg plan: 1) Create `crates/temper-server/src/observe/evolution/pipeline/` and move `insight_generator.rs` to `observe/evolution/pipeline/insight_pipeline.rs`; 2) Extract JSON projection/counting helpers from `records_list.rs` into `observe/evolution/pipeline/response_projection.rs`; 3) Update module exports in `observe/evolution.rs` and call sites; 4) Run import updates with `rg -n \"insight_generator|generate_insights|generate_unmet_intents|generate_feature_requests\" crates/temper-server/src` and fix paths; 5) Validate with `cargo check -p temper-server` and `cargo test -p temper-server observe::evolution`.",
      "confidence": "medium",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "evolution_domain_scatter"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Backend compatibility policy is not encoded as a single server-side abstraction, so docs and runtime drift by storage backend.",
      "Governance claims in top-level docs are stricter than restore-time implementation guarantees.",
      "Evolution functionality has grown without a single package owner boundary, causing domain scattering."
    ],
    "likely_symptoms": [
      "evolution_backend_contract_split",
      "verification_claims_exceed_restore_semantics",
      "evolution_domain_scattered_between_root_and_observe"
    ],
    "possible_false_positives": [
      "Blind-packet existing findings for assigned files include stale 'unused import'/'orphaned' signals that are not supported by current file contents (for example, `events.rs`, `records_list.rs`, and `init/mod.rs` are actively referenced and compile-shaped).",
      "Blind-packet structure fan-in/fan-out metrics are all 0.0 in directory profiles, so package scoring relied on directory shape/placement signals more than import-affinity thresholds."
    ]
  }
}