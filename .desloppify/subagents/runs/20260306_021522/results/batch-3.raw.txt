{
  "batch": "Cross-cutting Sweep",
  "batch_index": 3,
  "assessments": {
    "authorization_consistency": 74.0
  },
  "dimension_notes": {
    "authorization_consistency": {
      "evidence": [
        "`crates/temper-server/src/observe/agents.rs:88-92` and `:134-145` gate with `require_observe_auth` but forward user-supplied `params.tenant` directly to Turso queries without tenant scope derivation.",
        "`crates/temper-server/src/observe/evolution/records_list.rs:26-31` and `:82-85` perform auth checks but query global evolution lists with no tenant filtering argument.",
        "`crates/temper-server/src/events.rs:49-56` streams every broadcast event after auth check, while sibling stream handler `crates/temper-server/src/observe/entities.rs:187-201` applies `observe_tenant_scope` tenant filtering.",
        "`crates/temper-server/tests/dst_multi_tenant.rs:3-6` asserts tenant isolation as a core runtime invariant, but observe/event read paths above do not consistently enforce tenant-scoped reads."
      ],
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "confidence": "high"
    }
  },
  "findings": [
    {
      "dimension": "authorization_consistency",
      "identifier": "agent_audit_tenant_scope_bypass",
      "summary": "Agent audit endpoints trust caller-provided tenant filters instead of enforced tenant scope",
      "related_files": [
        "crates/temper-server/src/observe/agents.rs",
        "crates/temper-server/src/observe/entities.rs",
        "crates/temper-server/tests/dst_multi_tenant.rs"
      ],
      "evidence": [
        "`list_agents` and `get_agent_history` call `require_observe_auth` then pass `params.tenant.as_deref()` to Turso (`agents.rs:91-92`, `:142-143`) with no `observe_tenant_scope` or `extract_tenant` enforcement.",
        "Sibling observe endpoints explicitly derive/enforce tenant scope (`entities.rs:28`, `:187-191`), so this is inconsistent within the same API surface.",
        "Multi-tenant tests define isolation expectations (`dst_multi_tenant.rs:3-6`), making query-parameter-controlled tenant selection a risky trust boundary."
      ],
      "suggestion": "Derive tenant scope from auth context (same pattern as `observe_tenant_scope`) and intersect it with optional query filters. Reject mismatched tenant filters with `403`, and apply the enforced tenant to Turso queries in both agent handlers.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "observe_read_paths_missing_shared_tenant_scope_guard"
    },
    {
      "dimension": "authorization_consistency",
      "identifier": "evolution_records_not_tenant_scoped",
      "summary": "Evolution listing endpoints expose unscoped record sets after coarse auth check",
      "related_files": [
        "crates/temper-server/src/observe/evolution/records_list.rs",
        "crates/temper-server/src/observe/entities.rs",
        "README.md"
      ],
      "evidence": [
        "`list_evolution_records` and `list_evolution_insights` authorize once (`records_list.rs:26`, `:82`) but call state list methods with no tenant input (`:28-31`, `:84-85`).",
        "Comparable observe endpoints apply tenant filtering (`entities.rs:187-201`), showing missing consistency in sibling read paths.",
        "README positions authorization/governed access as default-deny mediation (`README.md:35`, `:70`, `:130-132`), but these handlers do not enforce tenant-level data boundaries in code path."
      ],
      "suggestion": "Add tenant scope extraction for evolution reads and plumb tenant-filtered query methods through `ServerState` and backing stores. Keep global listing only behind an explicit admin-only permission distinct from normal `read_evolution`.",
      "confidence": "medium",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "observe_read_paths_missing_shared_tenant_scope_guard"
    },
    {
      "dimension": "authorization_consistency",
      "identifier": "legacy_events_stream_leaks_cross_tenant",
      "summary": "`/tdata/$events` stream lacks tenant filtering unlike sibling event stream endpoint",
      "related_files": [
        "crates/temper-server/src/events.rs",
        "crates/temper-server/src/observe/entities.rs",
        "crates/temper-server/tests/dst_multi_tenant.rs"
      ],
      "evidence": [
        "`handle_events` subscribes and emits every `change` after `require_observe_auth` (`events.rs:49-56`) without tenant-scope checks.",
        "`handle_event_stream` in observe applies tenant filter from auth scope (`entities.rs:187-201`) for the same event type.",
        "Tenant isolation is treated as a core invariant in DST tests (`dst_multi_tenant.rs:3-6`), but this endpoint can still broadcast cross-tenant state transitions."
      ],
      "suggestion": "Apply `observe_tenant_scope` filtering to `handle_events` (same filtering logic as `handle_event_stream`) or deprecate `/tdata/$events` in favor of the scoped observe stream to avoid dual, inconsistent authorization behavior.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "observe_read_paths_missing_shared_tenant_scope_guard"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Tenant scoping is implemented ad hoc per handler instead of via a single mandatory observe-read guard abstraction.",
      "Legacy and newer observe/event endpoints evolved in parallel with inconsistent authorization depth (permission check present, data-scope check missing)."
    ],
    "likely_symptoms": [
      "agent_audit_tenant_scope_bypass",
      "evolution_records_not_tenant_scoped",
      "legacy_events_stream_leaks_cross_tenant"
    ],
    "possible_false_positives": [
      "If `require_observe_auth` internally enforces strict tenant-bound row-level policies in every backing query, impact is reduced; however, that guarantee is not evident in these reviewed files."
    ]
  }
}