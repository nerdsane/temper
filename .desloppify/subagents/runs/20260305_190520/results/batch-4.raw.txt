{
  "batch": "Package Organization",
  "batch_index": 4,
  "assessments": {
    "package_organization": 71.0,
    "high_level_elegance": 73.0
  },
  "dimension_notes": {
    "package_organization": {
      "evidence": [
        "holistic_context.structure.directory_profiles shows `crates/temper-server/src/` has `file_count: 22`, exceeding the batch threshold signal for flat-directory overload (>10 files with mixed concerns).",
        "`crates/temper-server/src/api.rs` is 1281 LOC and mixes unrelated endpoint families (secrets, policies, decision workflow, agent authz/audit, REPL) in one root file.",
        "Management router wiring in `api.rs` delegates multiple `/api/*` mutation routes to `crate::observe::*` handlers (`/specs/load-dir`, `/specs/load-inline`, `/wasm/modules/*`, `/evolution/*`), creating placement mismatch between route ownership and folder naming."
      ],
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "confidence": "high"
    },
    "high_level_elegance": {
      "evidence": [
        "`api.rs` states management endpoints are separated from read-only observe routes, but it immediately routes core mutations into `observe` modules, so the stated architectural contract is not trustworthy.",
        "`observe/evolution.rs` exports both read/query-style handlers and decision/mutation handlers (`handle_decide`, `handle_unmet_intent`, `handle_sentinel_check`), blurring ownership of read vs write behavior.",
        "`temper-observe/src/lib.rs` documents a query-store centered architecture but re-exports wide-event emission entry points at crate root, mixing producer and query-consumer concerns in the public surface."
      ],
      "impact_scope": "subsystem",
      "fix_scope": "architectural_change",
      "confidence": "medium"
    }
  },
  "findings": [
    {
      "dimension": "package_organization",
      "identifier": "observe_namespace_owns_api_mutations",
      "summary": "Mutation handlers for `/api` are physically placed under `observe`, violating folder-role boundaries.",
      "related_files": [
        "crates/temper-server/src/api.rs",
        "crates/temper-server/src/observe/evolution.rs",
        "crates/temper-server/src/observe/entities.rs"
      ],
      "evidence": [
        "`api.rs:1-5` claims management endpoints are separate from read-only observe routes.",
        "`api.rs:42-65` routes `/api/specs/*`, `/api/wasm/modules/*`, and `/api/evolution/*` to `crate::observe::*` handlers.",
        "`observe/evolution.rs:8-14` publicly re-exports mutation handlers used by `/api` (`handle_decide`, `handle_unmet_intent`, `handle_sentinel_check`) from the observe namespace."
      ],
      "suggestion": "Staged reorg plan: (1) create `crates/temper-server/src/api/` with `specs.rs`, `wasm.rs`, `evolution.rs`; (2) move `/api` mutation handlers out of `observe/*` into those files, keeping only read-side `/observe` endpoints in `observe/`; (3) update router imports in `api.rs` and `observe/mod.rs`; (4) run `rg \"crate::observe::(specs|wasm|evolution)::handle\" crates/temper-server/src`, then `cargo fmt` and `cargo test -p temper-server` to validate no stale imports.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "read_write_boundary_blur"
    },
    {
      "dimension": "package_organization",
      "identifier": "server_root_flattening_and_api_blob",
      "summary": "Root `temper-server/src` is overloaded and `api.rs` concentrates multiple unrelated domains.",
      "related_files": [
        "crates/temper-server/src/api.rs",
        "crates/temper-server/src/authz_helpers.rs",
        "crates/temper-server/src/constraint_engine.rs"
      ],
      "evidence": [
        "Directory profile reports `crates/temper-server/src/` has 22 production files while adjacent concerns already use subfolders (`observe/`, `state/`, `reaction/`, `entity_actor/`).",
        "`api.rs` is 1281 lines and contains separate phases/features (secrets, policy CRUD, decision workflow, agent authz/audit, REPL), indicating mixed ownership in one root module.",
        "`authz_helpers.rs` and `constraint_engine.rs` remain as root-level cross-cutting modules despite nearby domain folders where their concerns are consumed."
      ],
      "suggestion": "Staged reorg plan: (1) split `api.rs` into `api/secrets.rs`, `api/policies.rs`, `api/decisions.rs`, `api/agent.rs`, `api/repl.rs`; (2) introduce `mod api;` submodule routing assembly in a slim root `api.rs`; (3) move `authz_helpers.rs` to `security/authz_helpers.rs` and `constraint_engine.rs` to `state/constraints.rs` (or `invariants/constraints.rs`) with transitional re-exports; (4) run `rg \"crate::(authz_helpers|constraint_engine)\" crates/temper-server/src`, then `cargo fmt` and `cargo test -p temper-server`.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "architectural_change",
      "root_cause_cluster": "read_write_boundary_blur"
    },
    {
      "dimension": "high_level_elegance",
      "identifier": "observe_crate_surface_mixes_query_and_emission_roles",
      "summary": "`temper-observe` exposes query-store and telemetry-emission APIs at the same crate root, weakening role clarity.",
      "related_files": [
        "crates/temper-observe/src/lib.rs",
        "crates/temper-observe/src/clickhouse.rs",
        "crates/temper-observe/src/error.rs"
      ],
      "evidence": [
        "`lib.rs:8-11` describes architecture centered on `ObservabilityStore` query interfaces/adapters.",
        "`lib.rs:37` re-exports `emit_metrics`, `emit_span`, and `from_transition` from `wide_event`, introducing producer APIs into the same top-level surface as query/store interfaces.",
        "`clickhouse.rs` is strictly query-adapter code, reinforcing the mismatch between documented query-centric ownership and mixed root exports."
      ],
      "suggestion": "Staged reorg plan: (1) create explicit subdomains `query/` and `emit/` under `temper-observe/src`; (2) move `clickhouse`, `store`, `schema`, `memory` into `query/` and keep `wide_event`/`otel` under `emit/`; (3) change root `lib.rs` to re-export only one curated facade per subdomain (for example `pub mod query; pub mod emit;`), preserving compatibility via temporary deprecated re-exports; (4) run `rg \"temper_observe::(emit_|ClickHouseStore|ObservabilityStore)\" crates/` and `cargo test -p temper-observe` to validate import updates.",
      "confidence": "medium",
      "impact_scope": "module",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "surface_role_ambiguity"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Read/write boundary was encoded as naming intent (`observe` vs `api`) but not enforced by module placement.",
      "Growth-by-feature concentrated endpoints and cross-cutting utilities at crate root instead of enforcing subpackage ownership.",
      "Public crate surfaces are expanding by convenience re-exports without a strict role-oriented facade policy."
    ],
    "likely_symptoms": [
      "observe_namespace_owns_api_mutations",
      "server_root_flattening_and_api_blob"
    ],
    "possible_false_positives": [
      "The `temper-observe` mixed-surface finding may be partially intentional for ergonomics; if so, it should be made explicit in module naming/docs to avoid architectural ambiguity."
    ]
  }
}