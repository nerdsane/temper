{
  "batch": "Governance & Contracts",
  "batch_index": 5,
  "assessments": {
    "cross_module_architecture": 72.0,
    "test_strategy": 78.0,
    "package_organization": 87.0
  },
  "dimension_notes": {
    "cross_module_architecture": {
      "evidence": [
        "README.md documents Postgres/Turso parity and marks Observe dashboard as done, but crates/temper-server/src/observe/evolution/records_list.rs is Turso-only (`state.persistent_store()`) and returns empty payloads when Turso is absent or query fails.",
        "crates/temper-cli/src/serve/storage.rs provisions Postgres evolution persistence (`PostgresRecordStore::migrate`) while read-side Observe handlers in records_list.rs do not consume Postgres-backed evolution data.",
        "crates/temper-server/src/insight_generator.rs has two independent resolution paths (`generate_insights` vs `generate_unmet_intents`) with different `SubmitSpec` resolution rules, creating inconsistent governance semantics."
      ],
      "impact_scope": "subsystem",
      "fix_scope": "architectural_change",
      "confidence": "high"
    },
    "test_strategy": {
      "evidence": [
        "crates/temper-cli/src/init/mod.rs test `test_init_creates_directory_structure` intentionally passes an absolute filesystem path as `name` and asserts Cargo package content includes that path, which validates behavior that is invalid for Cargo package naming.",
        "The same init tests do not assert namespace validity under path-like input even though `to_namespace` transforms only `-`/`_`, allowing unresolved path separators to flow into generated namespace text.",
        "No tests in assigned files validate cross-backend evolution-read compatibility (Postgres vs Turso) for Observe endpoints despite runtime branching and backend-specific behavior."
      ],
      "impact_scope": "module",
      "fix_scope": "multi_file_refactor",
      "confidence": "high"
    },
    "package_organization": {
      "evidence": [
        "holistic_context.structure.directory_profiles shows `crates/temper-cli/src/serve/` concentrated into 3 files with 1634 LOC total; `storage.rs` itself mixes backend connection setup, migrations, registry restore, URL redaction, and persistence upsert logic.",
        "Within assigned files, evolution read concerns are split between server observe handlers and CLI storage bootstrap without a clear backend-neutral boundary module for evolution record access."
      ],
      "impact_scope": "module",
      "fix_scope": "multi_file_refactor",
      "confidence": "medium",
      "issues_preventing_higher_score": "`serve/storage.rs` currently centralizes multiple concerns that should be split into backend adapters and registry hydration units; this increases navigation cost and boundary ambiguity."
    }
  },
  "findings": [
    {
      "dimension": "cross_module_architecture",
      "identifier": "evolution_backend_contract_drift",
      "summary": "Observe evolution APIs are Turso-only despite documented Postgres/Turso backend contract",
      "related_files": [
        "README.md",
        "crates/temper-server/src/observe/evolution/records_list.rs",
        "crates/temper-cli/src/serve/storage.rs"
      ],
      "evidence": [
        "README.md states persistence is `Postgres or Turso/libSQL` and lists Observe dashboard as implemented.",
        "records_list.rs comments and implementation explicitly query Turso (`state.persistent_store()`) and return an empty successful response when Turso is unavailable or query fails.",
        "storage.rs sets up Postgres evolution persistence (`PostgresRecordStore::migrate`) but Observe read handlers do not expose that backend."
      ],
      "suggestion": "Introduce a backend-neutral evolution read contract in server state (for example `EvolutionReadStore`), implement Postgres and Turso adapters, and make `list_evolution_records`/`list_evolution_insights` call that interface. If the configured backend is unavailable, return a surfaced error (5xx) instead of silent empty data. Add integration tests for both backend modes (Turso-only and Postgres-only) covering successful and failure paths.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "architectural_change",
      "root_cause_cluster": "backend_contract_fragmentation"
    },
    {
      "dimension": "cross_module_architecture",
      "identifier": "unmet_intent_resolution_semantics_diverge",
      "summary": "Insight generation uses conflicting resolution rules for the same unmet-intent concept",
      "related_files": [
        "crates/temper-server/src/insight_generator.rs",
        "README.md"
      ],
      "evidence": [
        "`generate_insights` marks unresolved `EntitySetNotFound` as resolved if any `SubmitSpec` action exists for the entity type, without checking success status or ordering.",
        "`generate_unmet_intents` marks resolution only when `SubmitSpec` is successful and stores resolution timestamp.",
        "README positions Evolution Engine as a coherent O-P-A-D-I chain; divergent resolution semantics produce contradictory downstream records."
      ],
      "suggestion": "Extract one shared resolver for unmet-intent lifecycle semantics (status, success requirement, and time-ordering) and make both generators consume it. Define explicit contract tests with sequences that include failed `SubmitSpec`, out-of-order timestamps, and repeated submissions so both outputs stay consistent.",
      "confidence": "high",
      "impact_scope": "module",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "backend_contract_fragmentation"
    },
    {
      "dimension": "test_strategy",
      "identifier": "init_path_name_contract_masked_by_tests",
      "summary": "Init tests validate path-as-name behavior that masks invalid scaffold contracts",
      "related_files": [
        "crates/temper-cli/src/init/mod.rs",
        "README.md"
      ],
      "evidence": [
        "`run(name)` uses one input for directory path, Cargo package name template replacement, and namespace derivation.",
        "Test `test_init_creates_directory_structure` passes an absolute path as `name` and asserts `Cargo.toml` contains that absolute path, endorsing a non-portable package-name contract.",
        "`to_namespace` only splits on `-` and `_`, so path separators can pass through under path-like input while tests only check for `Namespace=\\\"Temper.` prefix."
      ],
      "suggestion": "Separate scaffold API inputs into `target_dir` and validated `project_name` (or derive `project_name` from basename), reject invalid Cargo/package and namespace characters, and update tests to assert normalized basename output. Add table-driven tests for relative name, absolute path target with explicit project name, and invalid name rejection.",
      "confidence": "high",
      "impact_scope": "module",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "contract_boundary_blur"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Backend capability contracts are documented at product level but encoded inconsistently at runtime boundaries.",
      "Core governance concepts (unmet intent resolution) are implemented in parallel paths without a single semantic source of truth.",
      "CLI scaffolding conflates filesystem location and domain identifier, and tests reinforce that conflation."
    ],
    "likely_symptoms": [
      "evolution_backend_contract_drift",
      "unmet_intent_resolution_semantics_diverge",
      "init_path_name_contract_masked_by_tests"
    ],
    "possible_false_positives": [
      "The review packet’s structural section does not include `root_files` or `coupling_matrix`, so package-organization conclusions rely primarily on `directory_profiles` and direct file inspection."
    ]
  }
}