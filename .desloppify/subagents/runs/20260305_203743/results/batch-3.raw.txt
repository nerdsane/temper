{
  "batch": "AI Debt & Migrations",
  "batch_index": 3,
  "assessments": {
    "incomplete_migration": 88.0
  },
  "dimension_notes": {
    "incomplete_migration": {
      "evidence": [
        "crates/temper-evolution/src/lib.rs line 27 uses `#[allow(deprecated)]` specifically to keep re-exporting `store::RecordStore` at the crate root.",
        "The same crate root simultaneously re-exports the newer record-chain-centric APIs (`chain`, `insight`, `records`, `PostgresRecordStore`), indicating old and new surfaces are still coexisting."
      ],
      "impact_scope": "module",
      "fix_scope": "multi_file_refactor",
      "confidence": "high",
      "issues_preventing_higher_score": "Deprecated public API is still intentionally exposed, so migration is not fully completed and downstream users can continue binding to legacy contracts."
    }
  },
  "findings": [
    {
      "dimension": "incomplete_migration",
      "identifier": "deprecated_record_store_reexport",
      "summary": "Crate root still exports deprecated `RecordStore`, prolonging legacy API usage.",
      "related_files": [
        "crates/temper-evolution/src/lib.rs",
        "crates/temper-evolution/src/records.rs"
      ],
      "evidence": [
        "`crates/temper-evolution/src/lib.rs` line 27 has `#[allow(deprecated)] pub use store::RecordStore;`, which bypasses deprecation warnings for consumers.",
        "The crate root already promotes newer APIs (`records::*`, `PostgresRecordStore`, `chain`, `insight`), so keeping the deprecated re-export preserves a dual-path API surface instead of forcing migration."
      ],
      "suggestion": "Phase out the deprecated re-export by removing `pub use store::RecordStore` from the crate root, provide a compatibility feature flag or explicit migration alias module for a bounded period, and update internal/downstream imports to concrete non-deprecated store types before deleting the shim.",
      "confidence": "high",
      "impact_scope": "subsystem",
      "fix_scope": "multi_file_refactor",
      "root_cause_cluster": "legacy_api_surface_kept_open"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Backward-compatibility was preserved via crate-root re-export instead of a time-bounded compatibility layer, allowing migration to stall."
    ],
    "likely_symptoms": [
      "deprecated_record_store_reexport"
    ],
    "possible_false_positives": []
  }
}